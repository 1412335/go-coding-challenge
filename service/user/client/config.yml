serviceName: "grpc-client"
version: "v1"
enableTracing: true
tracing:
  metrics: "prometheus"
clientConfig:
  user:
    version: "v1"
    serviceName: "srvs.user.v1"
    enableTracing: true
    grpc: 
      host: "v1"
      port: 9090
    enableTLS: true
    TLSCert:
      CACert : "./cert/ca-cert.pem"
# log:
#   #PANNIC, FATAL, ERROR, WARN, INFO, DEBUG
#   mode: "dev"
#   level: "DEBUG"
#   # levelTrace: "FATAL"
#   isLogFile: false
#   pathLogFile: "./logFile.log"